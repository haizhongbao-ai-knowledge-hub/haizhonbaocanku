# Knowledge Hub

这是《海中宝 AI 项目知识中枢》的细分目录。

每次导入 GPT 聊天或老板指令时，优先按下面顺序归档：

1. 原始内容先进 `GPT_OUTLINE_IMPORT.md` 或 `CHAT_RECORDS_INDEX.md`。
2. 已确认内容进入 `CONFIRMED_KNOWLEDGE.md`。
3. 未确认内容进入 `UNCONFIRMED_IDEAS.md`。
4. 可执行任务进入 `REQUIREMENTS_TABLE.md` 和 `TASKS.md`。
5. 重要判断进入 `DECISIONS.md`。

